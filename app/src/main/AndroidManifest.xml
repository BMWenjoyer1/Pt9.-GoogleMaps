<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos requeridos para acceder a la ubicación y Google Maps -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pt9GoogleMaps_Gonzalez_Adrian">

        <!-- Meta-data de Google Maps API -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity para búsqueda por coordenadas -->
        <activity
            android:name=".SearchByCoordinatesActivity"
            android:exported="false" />

        <!-- Activity para búsqueda por población -->
        <activity
            android:name=".SearchByLocationActivity"
            android:exported="false" />

        <!-- Activity para ubicación actual -->
        <activity
            android:name=".CurrentLocationActivity"
            android:exported="false" />
    </application>

</manifest>
